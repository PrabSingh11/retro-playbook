# Drafts

Pages that are **not** part of the published book but kept for possible future use.

Nothing in `index.html` links here, so these pages don't appear in the book. To
bring one back, move it into `chapters/` and re-add its `<li>` to the relevant
`index.html` shelf.

## Contents

- **match-the-icebreaker.html** — "Read the Room, Pick the Icebreaker", a Ch. 4
  match-up drill (situations ↔ icebreakers). Pulled from the book on request.
  Note: its `../assets/…` and `../index.html` paths still resolve from here, but
  the in-page chapter-nav links (`04-…`, `05-…`) assume it lives in `chapters/`.
